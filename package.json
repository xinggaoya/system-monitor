{
  "name": "system_monitor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "echo \"No frontend tests specified\" && exit 0",
    "type-check": "vue-tsc --noEmit",
    "lint": "echo \"No ESLint configuration\" && exit 0",
    "format": "echo \"No formatter configured\" && exit 0",
    "format:check": "echo \"No formatter configured\" && exit 0"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2.2",
    "@tauri-apps/plugin-opener": "^2.2",
    "pinia": "^2.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.7.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10",
    "@tauri-apps/cli": "^2.2"
  }
}
